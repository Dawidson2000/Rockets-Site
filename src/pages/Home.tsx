import { FC } from 'react';
import styled from 'styled-components';

const HomeW = styled.div`
  margin-top: 70px;
  color: white;
`;

const Home: FC = () => {

	return (
    <HomeW>
      HOME PAGE
    </HomeW>
  );
};

export default Home;
