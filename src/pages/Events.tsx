import { FC } from 'react';
import styled from 'styled-components';

const HomeW = styled.div`
  margin-top: 70px;
  color: white;
`;

const Events: FC = () => {

	return (
    <HomeW>
      Events
    </HomeW>
  );
};

export default Events;
